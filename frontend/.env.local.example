# Backend API base URL (used by the frontend to reach the FastAPI service)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: API auth token (must match backend's API_AUTH_TOKEN)
NEXT_PUBLIC_API_AUTH_TOKEN=
